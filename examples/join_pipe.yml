nodes:
  - command: [echo, one]
    outputs:
      - {fd: 1, name: into_cat}
  - command: [echo, two]
    outputs:
      - {fd: 1, name: into_cat}
  - command: [echo, three]
    outputs:
      - {fd: 1, name: into_cat}
  - command: [cat]
    inputs:
      - {fd: 0, name: into_cat}
